import { Bell, BriefcaseBusiness, CalendarDays, ChevronDown, ChevronLeft, ChevronRight, Heart, Image as ImageIcon, MapPin, Music2, Pencil, Play, Search, Settings, Star, Trash2, Upload, Video, X } from "lucide-react";
import { motion } from "framer-motion";
import { useMemo, useState } from "react";
import { Link } from "react-router-dom";

import Navbar from "../components/navbar/Navbar";
import Footer from "../components/footer/Footer";
import Button from "../components/ui/Button";
import SectionHeading from "../components/ui/SectionHeading";
import TalentSettingsPanel from "../components/talent/TalentSettingsPanel";
import talents from "../data/talents";

const mediaSeed = [
  { id: 1, title: "Featured performance", description: "A live vocal performance recorded during a recent production session.", type: "Video", year: 2026, likes: 184 },
  { id: 2, title: "Studio session", description: "A studio recording session focused on an intimate cinematic vocal arrangement.", type: "Audio", year: 2026, likes: 127 },
  { id: 3, title: "Behind the performance", description: "A quiet look at the preparation behind a feature-film recording.", type: "Image", year: 2025, likes: 96 },
  { id: 4, title: "Live stage performance", description: "Captured during a live performance with a full ensemble and backing musicians.", type: "Video", year: 2025, likes: 241 },
  { id: 5, title: "Original soundtrack", description: "Original vocal work created for an independent short-film soundtrack.", type: "Audio", year: 2025, likes: 168 },
  { id: 6, title: "Recording day", description: "From the recording floor during a collaborative studio production.", type: "Image", year: 2024, likes: 82 },
  { id: 7, title: "Acoustic session", description: "An acoustic interpretation recorded for a small independent project.", type: "Video", year: 2024, likes: 113 },
  { id: 8, title: "Vocal arrangement", description: "Layered vocal arrangement prepared for a cinematic score.", type: "Audio", year: 2023, likes: 74 },
  { id: 9, title: "On set", description: "A production still from a day spent recording vocals for screen.", type: "Image", year: 2023, likes: 61 },
];

const mediaIcon = { Video, Audio: Music2, Image: ImageIcon };

function OwnerControls({ onSettings, unread = 3 }) {
  return <div className="fixed right-5 top-24 z-40 flex items-center gap-1.5 rounded-full border border-white/10 bg-[#080808]/80 p-1.5 shadow-2xl backdrop-blur-xl sm:right-8"><button aria-label="Notifications" className="relative grid h-10 w-10 place-items-center rounded-full text-white/55 transition hover:bg-white/[0.06] hover:text-white"><Bell size={17} />{unread > 0 && <span className="absolute right-1.5 top-1 grid h-4 min-w-4 place-items-center rounded-full bg-amber-500 px-1 text-[8px] font-black text-black">{unread}</span>}</button><button aria-label="Settings" onClick={onSettings} className="grid h-10 w-10 place-items-center rounded-full text-white/55 transition hover:bg-amber-500 hover:text-black"><Settings size={17} /></button></div>;
}

function SectionAction({ children, onClick }) { return <button onClick={onClick} className="inline-flex items-center gap-2 rounded-full bg-white/5 px-4 py-2.5 text-xs font-semibold text-white/65 transition duration-300 hover:-translate-y-0.5 hover:bg-amber-500 hover:text-black hover:shadow-[0_0_28px_rgba(245,158,11,0.22)]">{children}</button>; }

function MediaCard({ item, talent, onEdit, onDelete }) {
  const Icon = mediaIcon[item.type];
  return <motion.article initial={{ opacity: 0, y: 50 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ duration: 0.65 }} whileHover={{ y: -6 }} className="group relative overflow-hidden rounded-[2rem] bg-white/[0.03] shadow-[0_20px_70px_rgba(0,0,0,0.22)]"><div className="relative aspect-[16/11] overflow-hidden bg-neutral-900"><img src={item.image || talent.image} alt="" className="h-full w-full object-cover transition duration-700 group-hover:scale-[1.045]" /><div className="absolute inset-0 bg-gradient-to-t from-black/90 via-black/15 to-transparent" /><div className="absolute left-4 top-4 flex items-center gap-2 rounded-full border border-white/15 bg-black/45 px-3.5 py-2 backdrop-blur-xl"><Icon size={12} className="text-amber-400" /><span className="text-[9px] font-medium uppercase tracking-[0.18em] text-white/75">{item.type}</span></div><span className="absolute right-4 top-4 rounded-full bg-black/40 px-3 py-2 text-[9px] text-white/50 backdrop-blur-xl">{item.year}</span>{item.type === "Video" && <span className="absolute bottom-5 left-5 grid h-12 w-12 place-items-center rounded-full bg-white/10 backdrop-blur-xl transition group-hover:bg-amber-500 group-hover:text-black"><Play size={15} fill="currentColor" /></span>}<div className="absolute bottom-4 right-4 flex gap-2 opacity-0 transition group-hover:opacity-100"><button aria-label="Edit media" onClick={() => onEdit(item)} className="grid h-9 w-9 place-items-center rounded-full bg-white/90 text-black transition hover:bg-amber-500"><Pencil size={14} /></button><button aria-label="Remove media" onClick={() => onDelete(item)} className="grid h-9 w-9 place-items-center rounded-full bg-amber-500 text-black transition hover:bg-amber-400"><Trash2 size={14} /></button></div></div><div className="p-5 sm:p-6"><div className="flex items-start justify-between gap-4"><div><h3 className="text-lg font-bold tracking-tight text-white">{item.title}</h3><p className="mt-2 line-clamp-2 text-sm leading-6 text-neutral-400">{item.description}</p></div><span className="text-xs font-bold text-white/15">{String(item.id).padStart(2, "0")}</span></div><div className="mt-6 flex items-center justify-between"><span className="text-[10px] uppercase tracking-[0.18em] text-white/25">{talent.craft}</span><span className="inline-flex items-center gap-1.5 text-xs text-white/35"><Heart size={13} />{item.likes}</span></div></div></motion.article>;
}

function Gallery({ talent }) {
  const [query, setQuery] = useState(""); const [type, setType] = useState("All"); const [sort, setSort] = useState("Latest"); const [visibleCount, setVisibleCount] = useState(6); const [media, setMedia] = useState(Array.isArray(talent.media) ? talent.media : mediaSeed); const [editing, setEditing] = useState(null);
  const filtered = useMemo(() => [...media].filter((item) => type === "All" || item.type === type).filter((item) => !query.trim() || `${item.title} ${item.description}`.toLowerCase().includes(query.trim().toLowerCase())).sort((a, b) => sort === "Latest" ? b.year - a.year || b.id - a.id : a.year - b.year || a.id - b.id), [media, type, query, sort]);
  const upload = (event) => { const file = event.target.files?.[0]; if (!file) return; setMedia((items) => [{ id: Date.now(), title: file.name.replace(/\.[^.]+$/, ""), description: "New portfolio work. Add a description and metadata.", type: file.type.startsWith("video") ? "Video" : file.type.startsWith("audio") ? "Audio" : "Image", year: new Date().getFullYear(), likes: 0 }, ...items]); };
  return <section id="work" className="mt-32 scroll-mt-24"><div className="flex flex-col gap-8 lg:flex-row lg:items-end lg:justify-between"><SectionHeading eyebrow="01 / My Gallery" title="The Work Speaks First" subtitle={`Performances, recordings and moments from ${talent.name}'s professional work.`} /><label className="inline-flex min-h-12 cursor-pointer items-center justify-center gap-3 rounded-full bg-amber-500 px-6 py-3 text-sm font-semibold text-black shadow-[0_0_40px_rgba(245,158,11,0.25)] transition hover:-translate-y-0.5 hover:bg-amber-400"><Upload size={17} />Upload Your Work<input type="file" accept="image/*,video/*,audio/*" className="sr-only" onChange={upload} /></label></div><div className="mt-10 flex flex-col gap-5 sm:flex-row sm:items-center sm:justify-between"><div className="flex flex-wrap gap-2">{["All", "Video", "Audio", "Image"].map((option) => <button key={option} onClick={() => { setType(option); setVisibleCount(6); }} className={`rounded-full px-4 py-2.5 text-xs font-semibold transition ${type === option ? "bg-amber-500 text-black" : "bg-white/5 text-white/45 hover:bg-white/10 hover:text-white"}`}>{option}</button>)}</div><div className="flex flex-col gap-4 sm:flex-row sm:items-center"><label className="flex w-full items-center gap-3 border-b border-white/10 py-2.5 sm:w-64"><Search size={16} className="text-white/30" /><input value={query} onChange={(event) => { setQuery(event.target.value); setVisibleCount(6); }} placeholder="Search media" className="w-full bg-transparent text-sm text-white outline-none placeholder:text-white/25" /></label><div className="relative"><select value={sort} onChange={(event) => setSort(event.target.value)} className="appearance-none rounded-full bg-white/5 py-2.5 pl-4 pr-9 text-xs text-white/70"><option>Latest</option><option>Oldest</option></select><ChevronDown size={13} className="pointer-events-none absolute right-3 top-1/2 -translate-y-1/2 text-white/35" /></div></div></div><div className="mt-10 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{filtered.slice(0, visibleCount).map((item) => <MediaCard key={item.id} item={item} talent={talent} onEdit={setEditing} onDelete={(selected) => setMedia((items) => items.filter((item) => item.id !== selected.id))} />)}</div>{visibleCount < filtered.length && <div className="mt-10 text-center"><button onClick={() => setVisibleCount((count) => count + 6)} className="rounded-full bg-white/5 px-7 py-3 text-xs font-semibold text-white/60 hover:bg-white/10 hover:text-white">More</button></div>}{editing && <MediaEditor item={editing} onClose={() => setEditing(null)} onSave={(next) => { setMedia((items) => items.map((item) => item.id === next.id ? next : item)); setEditing(null); }} />}</section>;
}

function MediaEditor({ item, onClose, onSave }) { const [draft, setDraft] = useState(item); return <div className="fixed inset-0 z-[90] grid place-items-center bg-black/70 p-5 backdrop-blur-sm"><div className="w-full max-w-xl rounded-3xl border border-white/10 bg-[#121212] p-6 shadow-2xl sm:p-8"><div className="flex items-center justify-between"><div><p className="text-[10px] uppercase tracking-[0.25em] text-amber-400">Media management</p><h3 className="mt-2 text-2xl font-bold text-white">Edit media</h3></div><button onClick={onClose} className="grid h-9 w-9 place-items-center rounded-full bg-white/5 text-white/50 hover:bg-white/10"><X size={16} /></button></div><div className="mt-7 space-y-4"><input value={draft.title} onChange={(event) => setDraft({ ...draft, title: event.target.value })} className="w-full rounded-xl border border-white/10 bg-white/5 px-4 py-3 text-sm text-white outline-none" placeholder="Title" /><textarea value={draft.description} onChange={(event) => setDraft({ ...draft, description: event.target.value })} rows={4} className="w-full resize-none rounded-xl border border-white/10 bg-white/5 px-4 py-3 text-sm text-white outline-none" placeholder="Description" /><div className="rounded-xl border border-white/10 bg-white/[0.025] p-4"><p className="text-xs font-semibold text-white/70">Visibility</p><p className="mt-1 text-xs text-white/30">Public — media uploaded to Pre Pro Post is always visible to everyone.</p></div></div><div className="mt-7 flex justify-end gap-3"><button onClick={onClose} className="rounded-full px-5 py-2.5 text-xs font-semibold text-white/50 hover:text-white">Cancel</button><button onClick={() => onSave(draft)} className="rounded-full bg-amber-500 px-6 py-2.5 text-xs font-bold text-black">Save changes</button></div></div></div>; }

function Experience({ talent, onManage }) { const entries = Array.isArray(talent.experiences) ? talent.experiences : []; const [start, setStart] = useState(0); const visible = entries.slice(start, start + 3); return <section id="experience" className="mt-32 scroll-mt-24"><div className="flex items-end justify-between gap-6"><SectionHeading eyebrow="02 / Experience" title="The Career Behind The Craft" subtitle={`A look at the experiences that shaped ${talent.name}'s professional journey.`} /><SectionAction onClick={onManage}><Pencil size={13} />Manage experience</SectionAction></div><div className={`mt-10 grid gap-5 ${entries.length > 2 ? "md:grid-cols-2 lg:grid-cols-3" : "md:grid-cols-2"}`}>{visible.map((entry, index) => <motion.article key={`${entry.company}-${entry.period}-${index}`} initial={{ opacity: 0, y: 50 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} className="relative min-h-[390px] overflow-hidden rounded-[2rem] border border-white/10 bg-white/[0.03] p-8 sm:p-10"><div className="grid h-16 w-16 place-items-center rounded-2xl border border-amber-500/20 bg-amber-500/10 text-amber-400"><BriefcaseBusiness size={28} /></div><p className="mt-10 text-xs font-semibold uppercase tracking-[0.24em] text-amber-400">{entry.period}</p><h3 className="mt-4 text-3xl font-black leading-tight text-white">{entry.title}</h3><p className="mt-2 text-sm text-neutral-400">{entry.company}</p><p className="mt-6 text-base leading-relaxed text-neutral-400">{entry.description}</p><span className="absolute bottom-8 left-8 rounded-full bg-white/5 px-4 py-2 text-xs font-semibold text-white/50">{entry.craft || talent.craft}</span></motion.article>)}</div>{entries.length > 3 && <div className="mt-8 flex justify-between"><p className="text-xs text-neutral-500">{start + 1}–{Math.min(start + 3, entries.length)} of {entries.length}</p><div className="flex gap-2"><button disabled={!start} onClick={() => setStart((value) => Math.max(0, value - 1))} className="grid h-10 w-10 place-items-center rounded-full bg-white/5 text-white/50 disabled:opacity-20"><ChevronLeft size={16} /></button><button disabled={start >= entries.length - 3} onClick={() => setStart((value) => Math.min(entries.length - 3, value + 1))} className="grid h-10 w-10 place-items-center rounded-full bg-white/5 text-white/50 disabled:opacity-20"><ChevronRight size={16} /></button></div></div>}</section>; }

function Availability({ onManage }) { const [cursor, setCursor] = useState(() => new Date()); const [selectedDay, setSelectedDay] = useState(null); const [states, setStates] = useState({}); const monthLabel = useMemo(() => new Intl.DateTimeFormat("en-US", { month: "long", year: "numeric" }).format(cursor), [cursor]); const { firstDay, daysInMonth } = useMemo(() => { const year = cursor.getFullYear(); const month = cursor.getMonth(); return { firstDay: new Date(year, month, 1).getDay(), daysInMonth: new Date(year, month + 1, 0).getDate() }; }, [cursor]); const today = useMemo(() => { const date = new Date(); date.setHours(0, 0, 0, 0); return date; }, []); const getStatus = (day) => { const date = new Date(cursor.getFullYear(), cursor.getMonth(), day); if (date < today) return "unavailable"; return states[date.toISOString().slice(0, 10)] || "available"; }; const meta = { available: { label: "Open", dot: "bg-amber-500", text: "text-amber-400" }, tentative: { label: "Tentative", dot: "bg-emerald-400", text: "text-emerald-400" }, unavailable: { label: "Unavailable", dot: "bg-white/20", text: "text-white/30" } }; const shift = (offset) => { setCursor((date) => new Date(date.getFullYear(), date.getMonth() + offset, 1)); setSelectedDay(null); }; const cycle = (day) => { const date = new Date(cursor.getFullYear(), cursor.getMonth(), day); if (date < today) return; const key = date.toISOString().slice(0, 10); setStates((current) => ({ ...current, [key]: current[key] === "tentative" ? "unavailable" : current[key] === "unavailable" ? "available" : "tentative" })); setSelectedDay(day); }; const selectedStatus = selectedDay ? getStatus(selectedDay) : null; return <section id="availability" className="mt-32 scroll-mt-24"><div className="flex flex-col gap-10 rounded-[2rem] bg-white/[0.03] p-7 sm:p-10 lg:flex-row lg:items-center lg:justify-between lg:p-12"><div className="max-w-xl"><div className="flex items-end justify-between gap-6"><SectionHeading eyebrow="03 / Availability" title="Find A Day That Works" subtitle="Your next 90 days are available by default. Mark days tentative or unavailable when needed." /><SectionAction onClick={onManage}><CalendarDays size={13} />Manage calendar</SectionAction></div><div className="mt-7 flex flex-wrap gap-5 text-xs font-semibold text-neutral-400"><span className="inline-flex items-center gap-2"><i className="h-2 w-2 rounded-full bg-amber-500" /> Open</span><span className="inline-flex items-center gap-2 text-emerald-300"><i className="h-2 w-2 rounded-full bg-emerald-400" /> Tentative</span><span className="inline-flex items-center gap-2 text-white/30"><i className="h-2 w-2 rounded-full bg-white/20" /> Unavailable</span></div>{selectedDay && <p className="mt-5 text-sm text-neutral-400">{monthLabel} {selectedDay} · <span className={meta[selectedStatus].text}>{meta[selectedStatus].label}</span></p>}</div><div className="w-full max-w-[400px] rounded-[2rem] bg-black/25 p-5 sm:p-6"><div className="flex items-center justify-between"><button onClick={() => shift(-1)} aria-label="Previous month" className="grid h-9 w-9 place-items-center rounded-full bg-white/5 text-white/50 hover:bg-white/10 hover:text-white"><ChevronLeft size={16} /></button><p className="text-sm font-bold text-white">{monthLabel}</p><button onClick={() => shift(1)} aria-label="Next month" className="grid h-9 w-9 place-items-center rounded-full bg-white/5 text-white/50 hover:bg-white/10 hover:text-white"><ChevronRight size={16} /></button></div><div className="mt-5 grid grid-cols-7 gap-1 text-center text-[9px] font-semibold uppercase tracking-wider text-white/25">{["S", "M", "T", "W", "T", "F", "S"].map((day, index) => <span key={`${day}-${index}`} className="py-1">{day}</span>)}</div><div className="mt-1 grid grid-cols-7 gap-1">{Array.from({ length: firstDay }).map((_, index) => <span key={`blank-${index}`} className="h-9" />)}{Array.from({ length: daysInMonth }, (_, index) => index + 1).map((day) => { const status = getStatus(day); const isSelected = selectedDay === day; return <button key={day} disabled={status === "unavailable" && new Date(cursor.getFullYear(), cursor.getMonth(), day) < today} onClick={() => cycle(day)} className={`group relative h-9 rounded-xl text-xs font-semibold transition ${status === "available" ? "text-amber-300 hover:bg-amber-500/10" : status === "tentative" ? "text-emerald-300 hover:bg-emerald-400/10" : "cursor-not-allowed text-white/15 line-through decoration-white/20"} ${isSelected ? "bg-white/10 ring-1 ring-white/20" : ""}`}><span>{day}</span><span className={`absolute bottom-1 left-1/2 h-1 w-1 -translate-x-1/2 rounded-full ${meta[status].dot}`} /></button>; })}</div><p className="mt-4 text-[10px] text-white/25">Only the next 90 days are managed. Past days are not editable.</p></div></div></section>; }

function SettingsHint({ onOpen }) { return <button onClick={onOpen} className="mt-5 inline-flex items-center gap-2 text-xs font-semibold text-white/35 transition hover:text-amber-400"><Settings size={13} />Manage everything from Settings</button>; }

export default function TalentProfileOwner() { const talent = talents[0]; const [settingsOpen, setSettingsOpen] = useState(false); const [settingsSection, setSettingsSection] = useState("professional"); const openSettings = (section = "professional") => { setSettingsSection(section); setSettingsOpen(true); }; const scrollTo = (id) => { setSettingsOpen(false); document.getElementById(id)?.scrollIntoView({ behavior: "smooth", block: "start" }); }; return <div className="min-h-screen overflow-hidden bg-[#050505] text-white"><Navbar /><OwnerControls onSettings={() => openSettings()} /><main className="px-6 pb-24 pt-32"><div className="mx-auto max-w-7xl"><Link to={`/talent?craft=${encodeURIComponent(talent.craft)}`} className="inline-flex items-center gap-2 text-sm text-neutral-500 hover:text-amber-400"><span>←</span> Back to {talent.craft}</Link><section className="mt-10 grid items-stretch gap-10 lg:grid-cols-[0.8fr_1.2fr] lg:gap-16"><motion.div initial={{ opacity: 0, scale: 0.98 }} animate={{ opacity: 1, scale: 1 }} transition={{ duration: 0.8 }} className="relative min-h-[560px] overflow-hidden rounded-[2.5rem] bg-neutral-900"><img src={talent.image} alt={talent.name} className="absolute inset-0 h-full w-full object-cover" /><div className="absolute inset-0 bg-gradient-to-t from-black via-black/10 to-transparent" /><span className="absolute bottom-6 left-6 rounded-full bg-amber-500 px-4 py-2 text-xs font-bold uppercase tracking-[0.14em] text-black">{talent.availability}</span></motion.div><motion.div initial={{ opacity: 0, x: 40 }} animate={{ opacity: 1, x: 0 }} transition={{ duration: 0.8, delay: 0.1 }} className="flex flex-col justify-center py-4 lg:py-10"><p className="text-xs font-semibold uppercase tracking-[0.4em] text-amber-400">{talent.craft}</p><h1 className="mt-5 text-5xl font-black leading-none tracking-tight md:text-7xl">{talent.name}</h1><div className="mt-7 flex flex-wrap gap-x-6 gap-y-3 text-base text-neutral-400"><span className="inline-flex items-center gap-2"><MapPin size={16} /> {talent.location}</span><span className="inline-flex items-center gap-2"><Star size={16} className="text-amber-400" /> 4.9 rating</span><span className="inline-flex items-center gap-2"><BriefcaseBusiness size={16} /> {talent.experience}</span></div><p className="mt-8 max-w-2xl text-lg leading-relaxed text-neutral-400">{talent.bio}</p><div className="mt-8 flex flex-wrap gap-2">{talent.genres.map((genre) => <span key={genre} className="rounded-full border border-amber-500/20 bg-amber-500/10 px-4 py-2 text-sm text-amber-300">{genre}</span>)}</div><div className="mt-10 flex flex-wrap gap-4"><Button className="inline-flex items-center gap-3">Preview public profile</Button><Button variant="secondary" onClick={() => openSettings()}>Open settings</Button></div><SettingsHint onOpen={() => openSettings()} /></motion.div></section><Gallery talent={talent} /><Experience talent={talent} onManage={() => openSettings("experience")} /><Availability onManage={() => openSettings("calendar")} /></div></main><Footer /><TalentSettingsPanel open={settingsOpen} onClose={() => setSettingsOpen(false)} initialSection={settingsSection} onSelect={scrollTo} /></div>; }
