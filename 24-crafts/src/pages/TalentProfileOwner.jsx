import { Bell, BriefcaseBusiness, CalendarDays, ChevronDown, ChevronLeft, ChevronRight, Heart, Image as ImageIcon, MapPin, Music2, Pencil, Play, Search, Settings, Star, Trash2, Upload, Video, X } from "lucide-react";
import { motion } from "framer-motion";
import { useMemo, useState } from "react";
import { Link } from "react-router-dom";

import Navbar from "../components/navbar/Navbar";
import Footer from "../components/footer/Footer";
import Button from "../components/ui/Button";
import SectionHeading from "../components/ui/SectionHeading";
import TalentSettingsPanel from "../components/talent/TalentSettingsPanel";
import talents from "../data/talents";

const mediaSeed = [
  { id: 1, title: "Featured performance", description: "A live vocal performance recorded during a recent production session.", type: "Video", year: 2026, likes: 184 },
  { id: 2, title: "Studio session", description: "A studio recording session focused on an intimate cinematic vocal arrangement.", type: "Audio", year: 2026, likes: 127 },
  { id: 3, title: "Behind the performance", description: "A quiet look at the preparation behind a feature-film recording.", type: "Image", year: 2025, likes: 96 },
  { id: 4, title: "Live stage performance", description: "Captured during a live performance with a full ensemble and backing musicians.", type: "Video", year: 2025, likes: 241 },
  { id: 5, title: "Original soundtrack", description: "Original vocal work created for an independent short-film soundtrack.", type: "Audio", year: 2025, likes: 168 },
  { id: 6, title: "Recording day", description: "From the recording floor during a collaborative studio production.", type: "Image", year: 2024, likes: 82 },
  { id: 7, title: "Acoustic session", description: "An acoustic interpretation recorded for a small independent project.", type: "Video", year: 2024, likes: 113 },
  { id: 8, title: "Vocal arrangement", description: "Layered vocal arrangement prepared for a cinematic score.", type: "Audio", year: 2023, likes: 74 },
  { id: 9, title: "On set", description: "A production still from a day spent recording vocals for screen.", type: "Image", year: 2023, likes: 61 },
];

const mediaIcon = { Video, Audio: Music2, Image: ImageIcon };

function OwnerControls({ onSettings, unread = 3 }) {
  return (
    <div className="fixed right-5 top-24 z-40 flex items-center gap-2 rounded-full border border-white/10 bg-black/65 p-1.5 shadow-2xl backdrop-blur-xl sm:right-8">
      <button aria-label="Notifications" className="relative grid h-10 w-10 place-items-center rounded-full text-white/60 transition hover:bg-white/10 hover:text-white"><Bell size={17} />{unread > 0 && <span className="absolute right-2 top-1.5 grid h-4 min-w-4 place-items-center rounded-full bg-amber-500 px-1 text-[8px] font-black text-black">{unread}</span>}</button>
      <button aria-label="Settings" onClick={onSettings} className="grid h-10 w-10 place-items-center rounded-full bg-white/5 text-white/65 transition hover:bg-amber-500 hover:text-black"><Settings size={17} /></button>
    </div>
  );
}

function SectionAction({ children, onClick }) {
  return <button onClick={onClick} className="inline-flex items-center gap-2 rounded-full bg-white/5 px-4 py-2.5 text-xs font-semibold text-white/65 transition hover:bg-amber-500 hover:text-black">{children}</button>;
}

function MediaCard({ item, talent, onEdit, onDelete }) {
  const Icon = mediaIcon[item.type];
  return <motion.article initial={{ opacity: 0, y: 50 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ duration: .65 }} whileHover={{ y: -6 }} className="group relative overflow-hidden rounded-[2rem] bg-white/[0.03] shadow-[0_20px_70px_rgba(0,0,0,.22)]">
    <div className="relative aspect-[16/11] overflow-hidden bg-neutral-900"><img src={item.image || talent.image} alt="" className="h-full w-full object-cover transition duration-700 group-hover:scale-[1.045]" /><div className="absolute inset-0 bg-gradient-to-t from-black/90 via-black/15 to-transparent" /><div className="absolute left-4 top-4 flex items-center gap-2 rounded-full border border-white/15 bg-black/45 px-3.5 py-2 backdrop-blur-xl"><Icon size={12} className="text-amber-400" /><span className="text-[9px] font-medium uppercase tracking-[.18em] text-white/75">{item.type}</span></div><span className="absolute right-4 top-4 rounded-full bg-black/40 px-3 py-2 text-[9px] text-white/50 backdrop-blur-xl">{item.year}</span>{item.type === "Video" && <span className="absolute bottom-5 left-5 grid h-12 w-12 place-items-center rounded-full bg-white/10 backdrop-blur-xl transition group-hover:bg-amber-500 group-hover:text-black"><Play size={15} fill="currentColor" /></span>}<div className="absolute bottom-4 right-4 flex gap-2 opacity-0 transition group-hover:opacity-100"><button onClick={() => onEdit(item)} className="grid h-9 w-9 place-items-center rounded-full bg-white/90 text-black"><Pencil size={14} /></button><button onClick={() => onDelete(item)} className="grid h-9 w-9 place-items-center rounded-full bg-red-500/90 text-white"><Trash2 size={14} /></button></div></div>
    <div className="p-5 sm:p-6"><div className="flex items-start justify-between gap-4"><div><h3 className="text-lg font-bold tracking-tight text-white">{item.title}</h3><p className="mt-2 line-clamp-2 text-sm leading-6 text-neutral-400">{item.description}</p></div><span className="text-xs font-bold text-white/15">{String(item.id).padStart(2,"0")}</span></div><div className="mt-6 flex items-center justify-between"><span className="text-[10px] uppercase tracking-[.18em] text-white/25">{talent.craft}</span><span className="inline-flex items-center gap-1.5 text-xs text-white/35"><Heart size={13}/>{item.likes}</span></div></div>
  </motion.article>;
}

function Gallery({ talent, onSettings }) {
  const [query, setQuery] = useState(""); const [type, setType] = useState("All"); const [sort, setSort] = useState("Latest"); const [visibleCount, setVisibleCount] = useState(6); const [media, setMedia] = useState(Array.isArray(talent.media) ? talent.media : mediaSeed); const [editing, setEditing] = useState(null);
  const filtered = useMemo(() => [...media].filter(i => type === "All" || i.type === type).filter(i => !query.trim() || `${i.title} ${i.description}`.toLowerCase().includes(query.trim().toLowerCase())).sort((a,b) => sort === "Latest" ? b.year-a.year || b.id-a.id : a.year-b.year || a.id-b.id), [media,type,query,sort]);
  const upload = (e) => { const file = e.target.files?.[0]; if (!file) return; setMedia(items => [{ id: Date.now(), title: file.name.replace(/\.[^.]+$/, ""), description: "New portfolio work. Add a description and metadata.", type: file.type.startsWith("video") ? "Video" : file.type.startsWith("audio") ? "Audio" : "Image", year: new Date().getFullYear(), likes: 0 }, ...items]); };
  const edit = item => setEditing(item);
  const remove = item => setMedia(items => items.filter(i => i.id !== item.id));
  return <section id="work" className="mt-32 scroll-mt-24"><div className="flex flex-col gap-8 lg:flex-row lg:items-end lg:justify-between"><SectionHeading eyebrow="01 / My Gallery" title="The Work Speaks First" subtitle={`Performances, recordings and moments from ${talent.name}'s professional work.`}/><label className="inline-flex min-h-12 cursor-pointer items-center justify-center gap-3 rounded-full bg-amber-500 px-6 py-3 text-sm font-semibold text-black shadow-[0_0_40px_rgba(245,158,11,.25)] transition hover:-translate-y-0.5 hover:bg-amber-400"><Upload size={17}/>Upload Your Work<input type="file" accept="image/*,video/*,audio/*" className="sr-only" onChange={upload}/></label></div><div className="mt-10 flex flex-col gap-5 sm:flex-row sm:items-center sm:justify-between"><div className="flex flex-wrap gap-2">{["All","Video","Audio","Image"].map(o => <button key={o} onClick={() => {setType(o);setVisibleCount(6)}} className={`rounded-full px-4 py-2.5 text-xs font-semibold transition ${type===o ? "bg-amber-500 text-black" : "bg-white/5 text-white/45 hover:bg-white/10 hover:text-white"}`}>{o}</button>)}</div><div className="flex flex-col gap-4 sm:flex-row sm:items-center"><label className="flex w-full items-center gap-3 border-b border-white/10 py-2.5 sm:w-64"><Search size={16} className="text-white/30"/><input value={query} onChange={e=>{setQuery(e.target.value);setVisibleCount(6)}} placeholder="Search media" className="w-full bg-transparent text-sm text-white outline-none placeholder:text-white/25"/></label><div className="relative"><select value={sort} onChange={e=>setSort(e.target.value)} className="appearance-none rounded-full bg-white/5 py-2.5 pl-4 pr-9 text-xs text-white/70"><option>Latest</option><option>Oldest</option></select><ChevronDown size={13} className="pointer-events-none absolute right-3 top-1/2 -translate-y-1/2 text-white/35"/></div></div></div><div className="mt-10 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{filtered.slice(0,visibleCount).map(item=><MediaCard key={item.id} item={item} talent={talent} onEdit={edit} onDelete={remove}/>)}</div>{visibleCount<filtered.length && <div className="mt-10 text-center"><button onClick={()=>setVisibleCount(c=>c+6)} className="rounded-full bg-white/5 px-7 py-3 text-xs font-semibold text-white/60 hover:bg-white/10 hover:text-white">More</button></div>}{editing && <MediaEditor item={editing} onClose={()=>setEditing(null)} onSave={next=>{setMedia(items=>items.map(i=>i.id===next.id?next:i));setEditing(null)}}/>}</section>;
}

function MediaEditor({ item, onClose, onSave }) { const [draft,setDraft]=useState(item); return <div className="fixed inset-0 z-[90] grid place-items-center bg-black/70 p-5 backdrop-blur-sm"><div className="w-full max-w-xl rounded-3xl border border-white/10 bg-[#121212] p-6 shadow-2xl sm:p-8"><div className="flex items-center justify-between"><div><p className="text-[10px] uppercase tracking-[.25em] text-amber-400">Media management</p><h3 className="mt-2 text-2xl font-bold text-white">Edit media</h3></div><button onClick={onClose} className="grid h-9 w-9 place-items-center rounded-full bg-white/5 text-white/50"><X size={16}/></button></div><div className="mt-7 space-y-4"><input value={draft.title} onChange={e=>setDraft({...draft,title:e.target.value})} className="w-full rounded-xl border border-white/10 bg-white/5 px-4 py-3 text-sm text-white outline-none" placeholder="Title"/><textarea value={draft.description} onChange={e=>setDraft({...draft,description:e.target.value})} rows={4} className="w-full resize-none rounded-xl border border-white/10 bg-white/5 px-4 py-3 text-sm text-white outline-none" placeholder="Description"/><div className="rounded-xl border border-white/10 bg-white/[.025] p-4"><p className="text-xs font-semibold text-white/70">Visibility</p><p className="mt-1 text-xs text-white/30">Public — media uploaded to Pre Pro Post is always visible to everyone.</p></div></div><div className="mt-7 flex justify-end gap-3"><button onClick={onClose} className="rounded-full px-5 py-2.5 text-xs font-semibold text-white/50 hover:text-white">Cancel</button><button onClick={()=>onSave(draft)} className="rounded-full bg-amber-500 px-6 py-2.5 text-xs font-bold text-black">Save changes</button></div></div></div>;
}

function Experience({ talent, onManage }) { const entries=Array.isArray(talent.experiences)?talent.experiences:[]; const [start,setStart]=useState(0); const visible=entries.slice(start,start+3); return <section id="experience" className="mt-32 scroll-mt-24"><div className="flex items-end justify-between gap-6"><SectionHeading eyebrow="02 / Experience" title="The Career Behind The Craft" subtitle={`A look at the experiences that shaped ${talent.name}'s professional journey.`}/><SectionAction onClick={onManage}><Pencil size={13}/>Manage experience</SectionAction></div><div className={`mt-10 grid gap-5 ${entries.length>2?"md:grid-cols-2 lg:grid-cols-3":"md:grid-cols-2"}`}>{visible.map((entry,index)=><motion.article key={`${entry.company}-${entry.period}-${index}`} initial={{opacity:0,y:50}} whileInView={{opacity:1,y:0}} viewport={{once:true}} className="relative min-h-[390px] overflow-hidden rounded-[2rem] border border-white/10 bg-white/[.03] p-8 sm:p-10"><div className="grid h-16 w-16 place-items-center rounded-2xl border border-amber-500/20 bg-amber-500/10 text-amber-400"><BriefcaseBusiness size={28}/></div><p className="mt-10 text-xs font-semibold uppercase tracking-[.24em] text-amber-400">{entry.period}</p><h3 className="mt-4 text-3xl font-black leading-tight text-white">{entry.title}</h3><p className="mt-2 text-sm text-neutral-400">{entry.company}</p><p className="mt-6 text-base leading-relaxed text-neutral-400">{entry.description}</p><span className="absolute bottom-8 left-8 rounded-full bg-white/5 px-4 py-2 text-xs font-semibold text-white/50">{entry.craft || talent.craft}</span></motion.article>)}</div>{entries.length>3&&<div className="mt-8 flex justify-between"><p className="text-xs text-neutral-500">{start+1}–{Math.min(start+3,entries.length)} of {entries.length}</p><div className="flex gap-2"><button disabled={!start} onClick={()=>setStart(s=>Math.max(0,s-1))} className="grid h-10 w-10 place-items-center rounded-full bg-white/5 text-white/50 disabled:opacity-20"><ChevronLeft size={16}/></button><button disabled={start>=entries.length-3} onClick={()=>setStart(s=>Math.min(entries.length-3,s+1))} className="grid h-10 w-10 place-items-center rounded-full bg-white/5 text-white/50 disabled:opacity-20"><ChevronRight size={16}/></button></div></div>}</section>; }

function Availability({ onManage }) { const [cursor,setCursor]=useState(new Date()); const [states,setStates]=useState({}); const year=cursor.getFullYear(),month=cursor.getMonth(); const first=new Date(year,month,1).getDay(),days=new Date(year,month+1,0).getDate(); const today=new Date();today.setHours(0,0,0,0); const statusFor=day=>{const d=new Date(year,month,day); if(d<today)return "past";return states[d.toISOString().slice(0,10)]||"available"}; const cycle=day=>{const d=new Date(year,month,day);if(d<today)return;const key=d.toISOString().slice(0,10);setStates(s=>({...s,[key]:s[key]==="tentative"?"unavailable":s[key]==="unavailable"?"available":"tentative"}))}; const meta={available:"bg-amber-500 text-amber-300",tentative:"bg-emerald-400 text-emerald-300",unavailable:"bg-white/20 text-white/25",past:"bg-transparent text-white/10"}; return <section id="availability" className="mt-32 scroll-mt-24"><div className="flex flex-col gap-10 rounded-[2rem] bg-white/[.03] p-7 sm:p-10 lg:flex-row lg:items-center lg:justify-between lg:p-12"><div className="max-w-xl"><div className="flex items-end justify-between gap-6"><SectionHeading eyebrow="03 / Availability" title="Find A Day That Works" subtitle="Your next 90 days are available by default. Mark days tentative or unavailable when needed."/><SectionAction onClick={onManage}><CalendarDays size={13}/>Manage calendar</SectionAction></div><div className="mt-7 flex flex-wrap gap-5 text-xs font-semibold text-neutral-400"><span>● Available</span><span className="text-emerald-300">● Tentative</span><span className="text-white/30">● Unavailable</span></div></div><div className="w-full max-w-[400px] rounded-[2rem] bg-black/25 p-5 sm:p-6"><div className="flex items-center justify-between"><button onClick={()=>setCursor(new Date(year,month-1,1))} className="grid h-9 w-9 place-items-center rounded-full bg-white/5 text-white/50"><ChevronLeft size={16}/></button><p className="text-sm font-bold text-white">{new Intl.DateTimeFormat("en-US",{month:"long",year:"numeric"}).format(cursor)}</p><button onClick={()=>setCursor(new Date(year,month+1,1))} className="grid h-9 w-9 place-items-center rounded-full bg-white/5 text-white/50"><ChevronRight size={16}/></button></div><div className="mt-5 grid grid-cols-7 gap-1 text-center text-[9px] font-semibold uppercase tracking-wider text-white/25">{["S","M","T","W","T","F","S"].map((d,i)=><span key={`${d}-${i}`} className="py-1">{d}</span>)}</div><div className="mt-1 grid grid-cols-7 gap-1">{Array.from({length:first}).map((_,i)=><span key={`blank-${i}`} className="h-9"/>)}{Array.from({length:days},(_,i)=>i+1).map(day=>{const s=statusFor(day);return <button key={day} onClick={()=>cycle(day)} disabled={s==="past"} className={`h-9 rounded-xl text-xs font-semibold transition hover:bg-white/10 ${meta[s]}`}><span>{day}</span><span className={`mx-auto mt-0.5 block h-1 w-1 rounded-full ${s==="available"?"bg-amber-500":s==="tentative"?"bg-emerald-400":s==="unavailable"?"bg-white/30":"bg-transparent"}`}/></button>})}</div><p className="mt-4 text-[10px] text-white/25">Only the next 90 days are managed. Past days are not editable.</p></div></div></section>; }

function SettingsHint({ onOpen }) { return <button onClick={onOpen} className="mt-5 inline-flex items-center gap-2 text-xs font-semibold text-white/35 transition hover:text-amber-400"><Settings size={13}/>Manage everything from Settings</button>; }

export default function TalentProfileOwner() { const talent=talents[0]; const [settingsOpen,setSettingsOpen]=useState(false); const scrollTo=id=>{setSettingsOpen(false);document.getElementById(id)?.scrollIntoView({behavior:"smooth",block:"start"})}; return <div className="min-h-screen overflow-hidden bg-[#050505] text-white"><Navbar/><OwnerControls onSettings={()=>setSettingsOpen(true)}/><main className="px-6 pb-24 pt-32"><div className="mx-auto max-w-7xl"><Link to={`/talent?craft=${encodeURIComponent(talent.craft)}`} className="inline-flex items-center gap-2 text-sm text-neutral-500 hover:text-amber-400"><span>←</span> Back to {talent.craft}</Link><section className="mt-10 grid items-stretch gap-10 lg:grid-cols-[.8fr_1.2fr] lg:gap-16"><motion.div initial={{opacity:0,scale:.98}} animate={{opacity:1,scale:1}} transition={{duration:.8}} className="relative min-h-[560px] overflow-hidden rounded-[2.5rem] bg-neutral-900"><img src={talent.image} alt={talent.name} className="absolute inset-0 h-full w-full object-cover"/><div className="absolute inset-0 bg-gradient-to-t from-black via-black/10 to-transparent"/><span className="absolute bottom-6 left-6 rounded-full bg-amber-500 px-4 py-2 text-xs font-bold uppercase tracking-[.14em] text-black">{talent.availability}</span></motion.div><motion.div initial={{opacity:0,x:40}} animate={{opacity:1,x:0}} transition={{duration:.8,delay:.1}} className="flex flex-col justify-center py-4 lg:py-10"><p className="text-xs font-semibold uppercase tracking-[.4em] text-amber-400">{talent.craft}</p><h1 className="mt-5 text-5xl font-black leading-none tracking-tight md:text-7xl">{talent.name}</h1><div className="mt-7 flex flex-wrap gap-x-6 gap-y-3 text-base text-neutral-400"><span className="inline-flex items-center gap-2"><MapPin size={16}/>{talent.location}</span><span className="inline-flex items-center gap-2"><Star size={16} className="text-amber-400"/>4.9 rating</span><span className="inline-flex items-center gap-2"><BriefcaseBusiness size={16}/>{talent.experience}</span></div><p className="mt-8 max-w-2xl text-lg leading-relaxed text-neutral-400">{talent.bio}</p><div className="mt-8 flex flex-wrap gap-2">{talent.genres.map(g=><span key={g} className="rounded-full border border-amber-500/20 bg-amber-500/10 px-4 py-2 text-sm text-amber-300">{g}</span>)}</div><div className="mt-10 flex flex-wrap gap-4"><Button className="inline-flex items-center gap-3">Preview public profile</Button><Button variant="secondary" onClick={()=>setSettingsOpen(true)}>Open settings</Button></div><SettingsHint onOpen={()=>setSettingsOpen(true)}/></motion.div></section><Gallery talent={talent}/><Experience talent={talent} onManage={()=>scrollTo("experience")}/><Availability onManage={()=>scrollTo("availability")}/></div></main><Footer/><TalentSettingsPanel open={settingsOpen} onClose={()=>setSettingsOpen(false)} onSelect={scrollTo}/></div>; }
